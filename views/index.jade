extends layout

block headAbove
    script(src='/socket.io/socket.io.js')

block content
    .header
        .container
            .row
                .col-sm-12
                    div.img.logo
                        img(src="/img/logo.png")
                    h1= title
                    p The Smart Grid Residential Load Balancer
    .container
        .row
            .col-sm-12
                .links
                    h2
                        a(href="/dashboard") Demo Page
  //p Power meter readings
  //div(id="windChart", ng-controller="windReadingsController")
  //  highchart(id="chart", config="chartConfig")
  //
  //div(id="windChart", ng-controller="readingsController")
  //  highchart(id="chart", config="chartConfig")
  //  div(ng-repeat="(key,val) in data")
  //    h2 {{key}}
  //    ul(ng-repeat="reading in val.readings")
  //      li {{reading.time}} - {{reading.data}}
  //
